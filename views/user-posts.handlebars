
        <section id="post-sect" class="d-flex flex-column w-50 m-auto mt-3 p-3 rounded">
            {{#if mappedPost}}
            {{#if (eq userInfo.username mappedPost.[0].user.username)}}
            <h2>All posts by you</h2>
            {{else}}
            <h2 class="m-auto mb-3">All posts by user {{mappedPost.[0].user.username}}</h2>
            {{/if}}
            {{else}}
            {{#if (eq userInfo.username noUser.username)}}
            <h2>You have no posts!</h2>
            {{else}}
            <h2>User {{noUser.username}} has no posts!</h2>
            {{/if}}
            {{/if}}
            {{#each mappedPost as |post|}}
            {{> post-partial}}
            {{/each}}
        </section>
        <script src="/js/homepage.js"></script>